# Hierarchical_clustringAnalysisOfMallCustomers
Hierarchical clustering is used to group data points based on similarity without requiring predefined labels. By applying distance-based merging and dendrogram visualization, the approach reveals natural data groupings and supports exploratory analysis when the optimal number of clusters is unknown
